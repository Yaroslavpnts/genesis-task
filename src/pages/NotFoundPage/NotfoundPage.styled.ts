import { styled } from "@mui/material";

export const NotFoundPageStyled = styled("div")`
  > div:first-of-type {
    font-family: "Mulish";
    font-style: normal;

    font-size: 30px;
    line-height: 38px;
    max-width: 450px;
    text-align: center;

    h3 {
      font-weight: 800;
      margin-bottom: 50px;
    }

    h4 {
      margin-bottom: 50px;
      font-size: 24px;
    }

    a {
      padding: 10px 75px;
      border-radius: 5px;
      font-weight: 800;
      color: #fff;
    }
  }
`;
